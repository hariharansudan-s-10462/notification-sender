{
  "name": "notification-sender",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:catalyst\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:catalyst": "catalyst serve",
    "build": "cd backend && npm run build:all",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../local-proxy-server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
